namespace Espeon.Core
{
    public enum Source
    {
        Commands,
        Discord,
        Pusharp
    }

    public enum Severity
    {
        Debug,
        Verbose,
        Info,
        Warning,
        Error,
        Critical
    }
}
