namespace Espeon
{
    public enum Source
    {
        Commands,
        Discord,
        Pusharp,
        Reminders
    }

    public enum Severity
    {
        Critical,
        Error,
        Warning,
        Info,
        Verbose,
        Debug
    }
}
